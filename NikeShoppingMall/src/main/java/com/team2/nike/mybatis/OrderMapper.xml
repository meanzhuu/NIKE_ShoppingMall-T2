<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="orders">
<!-- <select id="getList" resultType="ordersDto">
		SELECT *
		FROM	
			(SELECT result1.*, ROWNUM as rnum
			FROM
				(SELECT *
				FROM product
				ORDER BY product_id ASC) result1)
	</select> -->
	<select id="insert" parameterType="ordersDto">
		INSERT INTO orders
		(orders_id,	users_id, productDetail_orderDate, productDetail_deliveryState
		)VALUES(orders_seq.NEXTVAL,#{users_id},SYSDATE, #{productDetail_deliveryState})
	</select>
</mapper>